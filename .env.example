# Neon Postgres connection string (pooled)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# Secret Vercel injects into the cron Authorization header.
# Use the same value locally to smoke-test the cron route.
CRON_SECRET="generate-a-random-string-32-chars-min"
